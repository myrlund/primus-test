{
  "name": "primus-test",
  "version": "0.0.1",
  "description": "Testing running several primus servers in parallel on one server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "engine.io": "^1.5.1",
    "primus": "^2.4.12",
    "socket.io": "^0.9.13",
    "sockjs": "^0.3.12"
  }
}
